version: 2.1
jobs:
  build:
    docker:
      - image: cimg/node:18.9.1
    steps:
      - checkout
      - run: pwd
      - run: 
          name: Install dependencies
          command: npm install
      
